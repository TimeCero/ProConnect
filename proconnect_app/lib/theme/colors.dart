import 'package:flutter/material.dart';

class AppColors {
  // Colores principales (Azul oscuro + Naranja)
  static const Color primary = Color(0xFF1e3a5f);        // Azul oscuro (header)
  static const Color accent = Color(0xFFff7f3f);         // Naranja (botones principales)
  
  // Colores secundarios
  static const Color background = Color(0xFFF5F5F5);     // Fondo gris muy claro
  static const Color surface = Colors.white;             // Superficies blancas
  static const Color textPrimary = Color(0xFF1a1a1a);    // Texto oscuro
  static const Color muted = Color(0xFF757575);          // Texto gris
  
  // Colores adicionales
  static const Color success = Color(0xFF4CAF50);        // Verde
  static const Color error = Color(0xFFf44336);          // Rojo
  static const Color warning = Color(0xFFff9800);        // Naranja claro
  static const Color info = Color(0xFF2196f3);           // Azul claro
}
